{
    "dependencies": {
        "@aws-sdk/client-s3": "^3.1012.0",
        "@aws-sdk/s3-request-presigner": "^3.1012.0",
        "cookie-parser": "^1.4.7",
        "dotenv": "^17.3.1",
        "express": "^5.2.1",
        "jsonwebtoken": "^9.0.3",
        "pg": "^8.20.0"
    },
    "devDependencies": {
        "@aws-sdk/types": "^3.973.7",
        "@types/cookie-parser": "^1.4.10",
        "@types/express": "^5.0.6",
        "@types/jest": "^30.0.0",
        "@types/jsonwebtoken": "^9.0.10",
        "@types/node": "^25.5.0",
        "@types/pg": "^8.20.0",
        "ts-jest": "^29.4.6",
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.9.3"
    },
    "scripts": {
        "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
        "dk": "docker-compose up",
        "build": "tsc",
        "start": "node dist/index.js",
        "test": "jest",
        "testw": "jest --watch",
        "users": "docker exec -it my_postgres psql -U myuser -d mydb -c \"SELECT * FROM users LIMIT 5;\"",
        "followers": "docker exec -it my_postgres psql -U myuser -d mydb -c \"SELECT * FROM followers LIMIT 5;\"",
        "videos": "docker exec -it my_postgres psql -U myuser -d mydb -c \"SELECT * FROM videos LIMIT 5;\""
    }
}
